<ul class="navbar-nav mr-auto">
    {% for item in menu %}
        <li class="nav-item">
            <a class="nav-link" href="{{ item.link }}">{{ item.label }}</a>
        </li>
    {% endfor %}
</ul>