{% extends 'partials/_master.twig' %}

{% block main %}
    <h1>{{ record.title }}</h1>
    
    {{ fields() }}

    {% include 'partials/_recordfooter.twig' with {'extended': true} %}
{% endblock main %}